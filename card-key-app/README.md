# カードキー管理アプリ

会社のカードキー(6枚)の貸出・返却・利用履歴を管理するシンプルなWebアプリです。

## 機能

- カードキーごとの利用履歴一覧(利用者・利用日・返却予定日・返却日)
- 貸出登録・返却処理
- カードキー名 / 利用者名でのステータス検索(在庫があるかどうか)

## セットアップ

```sh
cd card-key-app
npm install
npm start
```

`http://localhost:4000` にアクセスしてください。

データは `data/db.json` に保存されます(初回起動時に自動生成)。
